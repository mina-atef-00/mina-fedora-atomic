updated
pacman-key --init
chaotic aur installation
copy pacman config/distrobox configs
installing packages
  gitbutler (sudo ln -s /usr/bin/gitbutler ~/usr/bin/gitbutler on distrobox then distrobox-export --bin /usr/bin/but on host)
  
exporting packages
